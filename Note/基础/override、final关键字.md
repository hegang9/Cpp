## override
由于派生类中virtual关键字可省略，若本意是写一个新函数，但是错误的将函数名与虚函数同名导致错误地重写；或者本意是重写，但是函数签名写错，导致错误地重新构建了一个虚函数。针对以上这两个没有编译期检查的隐含错误，我们可以使用override显式地指定我们的目的是重写基类虚函数，这允许编译器进行检查。

## final
禁止类被进一步继承，或禁止虚函数在派生类中被进一步重写。