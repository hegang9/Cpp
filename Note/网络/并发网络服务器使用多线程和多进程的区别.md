- 多进程（Multi-process）：
优点：进程间相互独立，拥有独立的虚拟地址空间，隔离性好。一个子进程崩溃通常不会影响其他进程或父进程；无需过于复杂的锁机制。
缺点：创建、销毁和上下文切换的开销很大；进程间通信（IPC如管道、共享内存等）较为复杂且低效。
- 多线程（Multi-thread）：
优点：线程属于同一个进程，共享代码段、数据段和堆，线程创建和切换代价远比进程低；相互通信非常方便（直接读写全局/共享变量）。
缺点：隔离性差，一个线程崩溃（例如段错误引发coredump）或内存越界会影响导致整个程序崩溃；存在资源竞争，必须小心仔细地使用锁（Mutex、自旋锁等）来同步。